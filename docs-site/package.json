{
  "name": "ejecs-docs",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "pages:dev": "wrangler pages dev .output/public --compatibility-date=2024-01-01",
    "pages:deploy": "npm run validate && wrangler pages deploy dist",
    "clean": "rm -rf .nuxt .output node_modules package-lock.json",
    "validate": "npm run build && node scripts/validate-build.js",
    "postinstall": "node scripts/check-versions.js",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix"
  },
  "dependencies": {
    "@nuxt/content": "2.8.5",
    "@tailwindcss/typography": "0.5.9",
    "nuxt": "3.8.1"
  },
  "devDependencies": {
    "@nuxtjs/tailwindcss": "6.8.0",
    "@cloudflare/kv-asset-handler": "0.3.4",
    "@cloudflare/workers-types": "4.20250410.0",
    "typescript": "5.0.4",
    "wrangler": "3.114.5"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "overrides": {
    "@cloudflare/kv-asset-handler": "0.3.4",
    "@cloudflare/workers-types": "4.20250410.0"
  }
} 