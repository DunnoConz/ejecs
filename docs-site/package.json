{
  "name": "ejecs-docs",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "pages:dev": "wrangler pages dev .output/public --compatibility-date=2025-04-09",
    "pages:deploy": "npm run validate && wrangler pages deploy .output/public",
    "clean": "rm -rf .nuxt .output node_modules package-lock.json",
    "validate": "npm run build && node scripts/validate-build.js",
    "postinstall": "node scripts/check-versions.js"
  },
  "dependencies": {
    "@nuxt/content": "2.9.0",
    "@tailwindcss/typography": "0.5.10",
    "nuxt": "3.9.0"
  },
  "devDependencies": {
    "@nuxtjs/tailwindcss": "6.10.0",
    "@cloudflare/kv-asset-handler": "0.3.4",
    "@cloudflare/workers-types": "4.20250410.0",
    "typescript": "5.3.0",
    "wrangler": "3.114.5"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "overrides": {
    "@cloudflare/kv-asset-handler": "0.3.4",
    "@cloudflare/workers-types": "4.20250410.0"
  }
} 